.DS_Store
# Real booking documents (names, confirmation numbers) — kept locally only,
# never committed to this public repo. The trip data model these were
# hydrated from lives in public/data/<trip>/*.json instead.
/attachments/

node_modules/
dist/
*.local
*.tsbuildinfo

# Playwright MCP session output (snapshots, screenshots, console logs)
.playwright-mcp/

# Plaintext API key file found sitting in the working tree — never commit it.
/trippin-api-key.txt
